services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data/cache:/app/data/cache

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
